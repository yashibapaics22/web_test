<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Note Taking App</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        .card:hover {
            transform: scale(1.05);
            transition: transform 0.3s ease-in-out;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }
        .delete-btn:hover {
            background-color: #e3342f;
            transition: background-color 0.3s;
        }
    </style>
</head>
<body class="bg-gray-100">
    <div class="bg-blue-600 text-white p-5 flex justify-between items-center shadow-md">
        <h1 class="text-2xl font-bold">📝 Note Taking App</h1>
        <div class="space-x-4">
            <a href="/" class="hover:text-gray-300">Home</a>
            <a href="/noteapp/new" class="hover:text-gray-300">Add New Note</a>
        </div>
    </div>

    <div class="container mx-auto mt-8 px-6">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
            <% for(let item of property){ %>
                <div class="bg-white p-4 rounded-lg shadow-lg card">
                    <img src="<%= item.imageUrl %>" class="w-full h-40 object-cover rounded-md" alt="Note Image">
                    <div class="mt-4">
                        <h2 class="text-xl font-semibold"><%= item.note %></h2>
                        <p class="text-gray-600 mt-2">Posted At: <%= item.createdAt.toDateString() %></p>
                        <div class="mt-4 flex space-x-3">
                            <a href="/noteapp/<%= item._id %>" class="bg-blue-500 text-white px-3 py-2 rounded hover:bg-blue-700">Edit</a>
                            <a href="/noteapp/<%= item._id %>/show" class="bg-green-500 text-white px-3 py-2 rounded hover:bg-green-700">View</a>
                            <form action="/noteapp/<%= item._id %>/delete?_method=DELETE" method="POST">
                                <button type="submit" class="delete-btn bg-red-500 text-white px-3 py-2 rounded">Delete</button>
                            </form>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
